1. prompt to control how agent behaves

- answer query immediately if possible
- answer query with knowledge base if needed
- escalate to human agent when
  a. query that needs knowledge base found 0 matching documents
  b. agent inconfident about the answer

1. structured response to ensure ChatResponse properly outputted by LLM

3. ReACT loop so that LLM can behave as agent

4. tool definitions
